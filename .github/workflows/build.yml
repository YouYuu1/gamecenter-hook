name: Build Tweak

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-13
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4

      - name: Install Theos (Automated)
        run: |
          # Oficjalny instalator Theos dla macOS - pobiera system i SDK automatycznie
          bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"

      - name: Build Tweak Package
        run: |
          export THEOS=~/theos
          
          # Generowanie plików filtra środowiska MobileLoader
          cat <<EOF > Filter.plist
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>Filter</key>
              <dict>
                  <key>Bundles</key>
                  <array>
                      <string>com.apple.gamecenter</string>
                  </array>
              </dict>
          </dict>
          </plist>
          EOF
          
          cp Filter.plist GameCenterHook.plist

          # Kompilacja projektu
          make package FINALPACKAGE=1

      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Tweak-Files
          path: |
            packages/*.deb
            .theos/obj/GameCenterHook.dylib
